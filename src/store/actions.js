import { GET_LIST_DATA, GET_DETAIL_DATA } from './type'
export default {
  // 异步的操作放在这里面
  [GET_LIST_DATA] (context, payload) {
    // console.log(content);
  },
  [GET_DETAIL_DATA] (context, payload) {}
}
